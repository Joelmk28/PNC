@page "/logout"
@inject CustomAuthenticationStateProvider AuthStateProvider
@inject NavigationManager Navigation
@using PNC.Services

<PageTitle>Déconnexion - PNC</PageTitle>

<div class="loading-container">
    <div class="loader-spinner"></div>
</div>

@code {
    protected override async Task OnInitializedAsync()
    {
        try
        {
            Console.WriteLine("🔓 Début de la déconnexion...");
            
            // Déconnexion via le CustomAuthenticationStateProvider
            await AuthStateProvider.SignOutAsync();
            
            Console.WriteLine("✅ Déconnexion réussie");
            
            // Attendre un peu pour que l'utilisateur voie le message
            await Task.Delay(1500);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"❌ Erreur lors de la déconnexion: {ex.Message}");
        }
        finally
        {
            // Rediriger vers la page de connexion
            Console.WriteLine("🔄 Redirection vers la page de connexion...");
            Navigation.NavigateTo("/login");
        }
    }
}

